#include "Input.hpp"
#include "MainMenu.h"


void Input(Data* _data, MainMenu *_mainMenu)
{
	/*sf::Event event;
	while (_data->system.window.pollEvent(event))
	{
		if (event.type == sf::Event::Closed)
			_data->system.window.close();
	}*/

	//_mainMenu->Input(_data);
}